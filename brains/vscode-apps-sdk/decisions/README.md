# Decisions

One file per decision that must not be committed to `integromat/vscode-apps-sdk`: `YYYY-MM-DD-short-title.md`.

```markdown
# Title

- Decision:
- Why:
- Next time:
```

Architecture that belongs to the extension itself stays in that repo's `AGENTS.md`.
