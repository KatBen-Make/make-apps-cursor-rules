# Decisions

One file per decision: `YYYY-MM-DD-short-title.md`.

```markdown
# Title

- Decision:
- Why:
- Next time:
```

Put a habit that applies in every workspace into `rules/` in this repo, not here.
